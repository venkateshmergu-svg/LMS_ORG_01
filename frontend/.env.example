# ============================================================
# LMS FRONTEND ENVIRONMENT VARIABLES
# ============================================================
# This file documents ALL build-time environment variables.
# Copy to .env.local for local development.
#
# IMPORTANT: 
# - All variables MUST be prefixed with VITE_ to be exposed
# - NO SECRETS in frontend (client-side code is public)
# - Values are BAKED IN at build time (immutable)
# ============================================================

# ------------------------------------------------------------
# API CONFIGURATION
# ------------------------------------------------------------
# Base URL for the LMS backend API
# Format: https://api.example.com (no trailing slash)
VITE_API_BASE_URL=http://localhost:8000

# ------------------------------------------------------------
# AUTHENTICATION / OAUTH
# ------------------------------------------------------------
# OAuth 2.0 / OpenID Connect Client ID
# This is a PUBLIC identifier (not a secret)
VITE_OAUTH_CLIENT_ID=your-client-id

# OAuth Authority / Issuer URL
# The identity provider's base URL
VITE_OAUTH_AUTHORITY=http://localhost:8000

# OAuth Redirect URI (after login)
VITE_OAUTH_REDIRECT_URI=http://localhost:5173/auth/callback

# OAuth Post-Logout Redirect URI
VITE_OAUTH_POST_LOGOUT_REDIRECT_URI=http://localhost:5173

# ------------------------------------------------------------
# FEATURE FLAGS (Read-Only)
# ------------------------------------------------------------
# Enable/disable features at build time
# Use "true" or "false" (strings, not booleans)
VITE_FEATURE_CALENDAR_SYNC=false
VITE_FEATURE_HRIS_INTEGRATION=false
VITE_FEATURE_PAYROLL_EXPORT=false
VITE_FEATURE_ADVANCED_REPORTS=false

# ------------------------------------------------------------
# APPLICATION METADATA
# ------------------------------------------------------------
# Application name displayed in UI
VITE_APP_NAME=Leave Management System

# Support/help email
VITE_SUPPORT_EMAIL=support@company.com

# ------------------------------------------------------------
# BUILD METADATA (Injected by CI/CD)
# ------------------------------------------------------------
# These are typically set during CI build, not manually
# VITE_BUILD_VERSION=1.0.0
# VITE_BUILD_COMMIT=abc123
# VITE_BUILD_DATE=2025-01-01T00:00:00Z
# VITE_BUILD_NUMBER=123
